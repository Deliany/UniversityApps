<UserControl x:Name="testControl" x:Class="EducationalTrainer.TestSystem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <WebBrowser x:Name="webBrowser" Grid.Row="0" VerticalAlignment="Top" MinHeight="300"/>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" Grid.Row="1" VerticalScrollBarVisibility="Auto" MinHeight="100">
            <Border CornerRadius="6" BorderBrush="Gray" Background="LightGray" BorderThickness="2" >
        <StackPanel x:Name="stackPanel" />
                </Border>
         </ScrollViewer>
    </Grid>
</UserControl>
